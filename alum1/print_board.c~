#include "header.h"

void    print_board(int	*array, int *number)
{
    int i;
	int j;
	
	j = 0;
	while (j < *number)
	{
		i = 0;
		while (i < *array)
		{
			write(1, "|", 1);
			i++;
		}
		write(1, "\n", 1);
		array++;
		j++;
	}
}
